CredsCache (main src: fs/overlayfs) 
=================================================================
sudo make
sudo make INSTALL_MOD_STRIP=1 modules_install &&sudo make install

* Reboot and select Linux5.15.6-CachedOFS-GC
* Use the filesystem benchmarks (e.g. FxMark) on the Docker instance to measure the performance of CredsCache


=================================================================
